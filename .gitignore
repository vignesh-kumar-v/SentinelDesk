# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.venv-*/
venv/

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# secrets
.env

# Trained checkpoints are large and reproducible from the pairs plus the recorded
# config; the training *history* is the artifact worth keeping in the repo, since it
# is what the results report reads and what a reader would want to check.
artifacts/**/checkpoint/
artifacts/**/*.safetensors
artifacts/**/*.bin
artifacts/**/*.pt
artifacts/**/*.gguf
models/
data/raw/
data/cache/

# vllm build tree
third_party/vllm/

# run logs: megabytes of progress-bar carriage returns, regenerable, and the
# findings that matter from them are written up in docs/decisions.md
reports/*.log

# os
.DS_Store
